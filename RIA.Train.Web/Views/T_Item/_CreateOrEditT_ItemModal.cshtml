
@using Abp.Extensions
@using Abp.Application.Services.Dto
@using RIA.Train.Web.Models.Common
@using RIA.Train.Web.Models

@model CreateOrEditT_ItemModalViewModel

@* 初始化头部 *@
@Html.Partial("~/Views/Common/Modals/_ModalHeader.cshtml", new ModalHeaderViewModel(Model.IsEditMode ? (L("EditT_Item") + ": " + Model.T_Item.ProjectName) : L("CreateT_Item")))


<!--html代码区域-->
<div class="modal-body">
    <form name="t_ItemInformationsForm"   role="form" novalidate class="form-validation">
 <input type="hidden" name="Id" value="@Model.T_Item.Id" />
 <div class="row>
            
               
	     <div hidden="hidden">           
            @if (Model.IsEditMode)
            {
                <input type="hidden" name="Id" value="@Model.T_Item.Id"/>
            }
        </div>
	                  
               
  	<div class="form-group form-md-line-input form-md-floating-label  ">
            <input class="maxlength-handler form-control @(Model.T_Item.ProjectName.IsNullOrEmpty() ? "" : " edited")" type="text" name="ProjectName" value="@Model.T_Item.ProjectName"		maxlength="30"  />			
            <label>@L("培训项目名称") </label>
        </div>
   
	                  
               
	    @*<div class="form-group form-md-line-input form-md-floating-label no-hint">
            <label>@L("CreateTime"):   </label>            
                <div class="input-group  date form_datetime">
                    <input type="text" name="CreateTime" value="@Model.T_Item.CreateTime" readonly class="form-control">
                    <span class="input-group-btn">
                        <button class="btn default date-set" type="button">
                            <i class="fa fa-calendar"></i>
                        </button>
                    </span>
                </div>
        
        </div>*@
	                  
            </div>
    </form>
</div>
@Html.Partial("~/Views/Common/Modals/_ModalFooterWithSaveAndCancel.cshtml")
 
